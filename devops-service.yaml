apiVersion: v1
kind: Service
metadata:
  name: devops-service
spec:
  type: NodePort
  selector:
    app: node-app         # Must match the Deployment label
  ports:
    - port: 3000          # service port
      targetPort: 3000     # container port
      nodePort: 30081      # any free NodePort (30000–32767)
